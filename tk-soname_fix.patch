--- tk8.4.3/unix/Makefile.in.wiget	2003-06-11 09:17:16.000000000 +0200
+++ tk8.4.3/unix/Makefile.in	2003-06-11 09:18:20.000000000 +0200
@@ -403,7 +403,8 @@
 # a shared library or non-shared library for Tk.
 ${LIB_FILE}: ${OBJS}
 	rm -f $@
-	@MAKE_LIB@
+	@MAKE_LIB@ -Wl,-soname,libtk${VERSION}.so.0
+	-ln -sf ${LIB_FILE} libtk${VERSION}.so
 
 ${STUB_LIB_FILE}: ${STUB_LIB_OBJS}
 	rm -f $@
--- tk8.4.3/unix/tcl.m4.wiget	2003-06-11 09:17:16.000000000 +0200
+++ tk8.4.3/unix/tcl.m4	2003-06-11 09:17:16.000000000 +0200
@@ -1096,7 +1096,7 @@
 	Linux*)
 	    SHLIB_CFLAGS="-fPIC"
 	    SHLIB_LD_LIBS='${LIBS}'
-	    SHLIB_SUFFIX=".so"
+	    SHLIB_SUFFIX=".so.0.0"
 
 	    # egcs-2.91.66 on Redhat Linux 6.0 generates lots of warnings 
 	    # when you inline the string and math operations.  Turn this off to
